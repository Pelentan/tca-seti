apiVersion: apps/v1
kind: Deployment
metadata:
  name: plot-test
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "seti.labels" . | nindent 4 }}
    app: plot-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plot-test
  template:
    metadata:
      labels:
        app: plot-test
        {{- include "seti.labels" . | nindent 8 }}
    spec:
      initContainers:
        {{- include "seti.waitForCertForge" . | nindent 8 }}
        {{- include "seti.waitForAugurCanis" . | nindent 8 }}
        {{- include "seti.waitForPlotStore" . | nindent 8 }}
        {{- include "seti.waitForSignalAggregator" . | nindent 8 }}
        {{- include "seti.waitForResults" . | nindent 8 }}
        {{- include "seti.waitForInteractions" . | nindent 8 }}
      containers:
        - name: plot-test
          image: {{ .Values.registry }}/{{ .Values.imagePrefix }}plot-test:{{ .Values.plotTest.tag }}
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.plotTest.port }}
          env:
            - name: PORT
              value: {{ .Values.plotTest.port | quote }}
            - name: REDIS_URL
              value: {{ .Values.redis.url | quote }}
            - name: SERVICE_NAME
              value: "plot-test"
            - name: PUBLIC_PORT
              value: {{ .Values.certForge.publicPort | quote }}
            - name: ENROLLMENT_CERT
              value: "/certs/enrollment.crt"
            - name: ENROLLMENT_KEY
              value: "/certs/enrollment.key"
            - name: ENROLLMENT_CA
              value: "/certs/enrollment-ca.crt"
            - name: CERT_FORGE_URL
              value: "https://cert-forge:{{ .Values.certForge.port }}"
            - name: ENROLLMENT_PORT
              value: {{ .Values.certForge.enrollmentPort | quote }}
            - name: AUGUR_CANIS_URL
              value: "https://augur-canis:{{ .Values.augurCanis.port }}"
            - name: HOSTNAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OBSERVABILITY_URL
              value: "https://seti-observability:{{ .Values.setiObservability.port }}"
            - name: PLOT_STORE_URL
              value: "https://plot-store:{{ .Values.plotStore.port }}"
            - name: SIGNAL_AGGREGATOR_URL
              value: "https://signal-aggregator:{{ .Values.signalAggregator.port }}"
            - name: RESULTS_URL
              value: "https://results:{{ .Values.results.port }}"
            - name: INTERACTIONS_URL
              value: "https://interactions:{{ .Values.interactions.port }}"
            - name: POLICY_URL
              value: "https://policy:{{ .Values.policy.port }}"
          volumeMounts:
            - name: seti-certs
              mountPath: /certs
              readOnly: true
          readinessProbe:
            exec:
              command: ["/healthcheck"]
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 5
          livenessProbe:
            exec:
              command: ["/healthcheck"]
            initialDelaySeconds: 45
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
      volumes:
        - name: seti-certs
          secret:
            secretName: {{ .Values.certSecretName }}
---
apiVersion: v1
kind: Service
metadata:
  name: plot-test
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "seti.labels" . | nindent 4 }}
    app: plot-test
spec:
  selector:
    app: plot-test
  ports:
    - port: {{ .Values.plotTest.port }}
      targetPort: {{ .Values.plotTest.port }}
