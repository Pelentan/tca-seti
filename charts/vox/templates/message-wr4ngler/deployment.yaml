apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-wr4ngler
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "vox.labels" . | nindent 4 }}
    app: message-wr4ngler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: message-wr4ngler
  template:
    metadata:
      labels:
        app: message-wr4ngler
        {{- include "vox.labels" . | nindent 8 }}
    spec:
      initContainers:
        {{- include "vox.waitForCertForge" . | nindent 8 }}
        {{- include "vox.waitForObservability" . | nindent 8 }}
        {{- include "vox.waitForAugurCanis" . | nindent 8 }}
      containers:
        - name: message-wr4ngler
          image: {{ .Values.registry }}/message-wr4ngler:{{ .Values.messageWr4ngler.tag }}
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.messageWr4ngler.port }}
          env:
            - name: PORT
              value: {{ .Values.messageWr4ngler.port | quote }}
            - name: SERVICE_NAME
              value: "message-wr4ngler"
            - name: CERT_FORGE_URL
              value: "https://cert-forge:{{ .Values.certForge.port }}"
            - name: ENROLLMENT_PORT
              value: {{ .Values.certForge.enrollmentPort | quote }}
            - name: PUBLIC_PORT
              value: {{ .Values.certForge.publicPort | quote }}
            - name: ENROLLMENT_CERT
              value: "/certs/enrollment.crt"
            - name: ENROLLMENT_KEY
              value: "/certs/enrollment.key"
            - name: ENROLLMENT_CA
              value: "/certs/enrollment-ca.crt"
            - name: AUGUR_CANIS_URL
              value: "https://augur-canis:{{ .Values.augurCanis.port }}"
            - name: REDIS_URL
              value: "redis:6379"
            - name: HOSTNAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OBSERVABILITY_URL
              value: "https://observability-service:{{ .Values.observabilityService.port }}"
          volumeMounts:
            - name: vox-certs
              mountPath: /certs
              readOnly: true
          livenessProbe:
            exec:
              command: ["/healthcheck"]
            initialDelaySeconds: 45
            periodSeconds: 30
            failureThreshold: 3
      volumes:
        - name: vox-certs
          secret:
            secretName: {{ .Values.certSecretName }}
---
apiVersion: v1
kind: Service
metadata:
  name: message-wr4ngler
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "vox.labels" . | nindent 4 }}
    app: message-wr4ngler
spec:
  selector:
    app: message-wr4ngler
  ports:
    - port: {{ .Values.messageWr4ngler.port }}
      targetPort: {{ .Values.messageWr4ngler.port }}
