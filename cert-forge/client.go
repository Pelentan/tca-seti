package main

// This file is intentionally empty — the client pattern lives in each
// service's certforge.go. It exists to document that cert-forge itself
// does not need a client (it IS the server).
